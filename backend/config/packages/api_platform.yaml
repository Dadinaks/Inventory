api_platform:
    title: Inventory API
    version: 1.0.0
    defaults:
        stateless: true
        cache_headers:
            vary: ["Content-Type", "Authorization", "Origin"]
        formats:
            json: ["application/json"]
            jsonld: ["application/ld+json"]
